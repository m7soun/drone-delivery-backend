meta {
  name: Withdraw Order
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/orders/:orderId
  body: none
  auth: bearer
}

params:path {
  orderId: REPLACE_WITH_ORDER_ID
}

auth:bearer {
  token: {{enduserToken}}
}
